<?xml version="1.0" encoding="UTF-8"?>
<configuration  scan="true" scanPeriod="60 seconds" debug="false">

    <property name="SERVER_NAME" value="springboot-consumer" />
    <springProperty scop="context" name="LOG_HOME" source="loggingx.path" />

    <contextName>${loggingx.contextName}</contextName>

    <!-- 默认的控制台日志输出，一般生产环境都是后台启动，这个没太大作用 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>[${SERVER_NAME}][%d{yyyy-MM-dd-HH:mm:ss}]|%-5level|%X{traceId:-0001}|[%thread]|%logger{80}.%M:%L| %msg%n</pattern>
        </encoder>
    </appender>


    <appender name="DEMO_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <append>true</append>

        <!--控制日志输出级别-->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_HOME}/springboot-consumer-%d{yyyy-MM-dd}.log</FileNamePattern>
            <MaxHistory>30</MaxHistory>
        </rollingPolicy>

        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>[%d{yyyy-MM-dd-HH:mm:ss}]|%-5level|%X{traceId:-0001}|[%thread]|%logger{80}.%M:%L| %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>


    <root level="debug">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="DEMO_LOG"/>
    </root>
</configuration>